# SwipeSort — APK terbaru

Unduh `SwipeSort-debug.apk` lalu pasang di Android
(izinkan "install dari sumber tidak dikenal" saat diminta).

Branch ini dibuat otomatis oleh GitHub Actions dari setiap build.
